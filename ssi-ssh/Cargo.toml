[package]
name = "ssi-ssh"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[features]
default = ["p256"]
p256 = ["ssi-jwk/secp256k1"]

[dependencies]
thiserror = "1.0"
ssi-jwk = { path = "../ssi-jwk", version = "0.1" }
sshkeys = "0.3"
